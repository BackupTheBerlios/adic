#include "playerdata.h"

PlayerData::PlayerData()
  : maxspeed(1),
    minspeed(1),
    rotspeed(1),
    acceleration(1),
    hdamping(1),
    vdamping(1),
    mass(1),
    maxfitness(1),
    minfitness(1),
    endurance(1),
    type(0),
    r(1),
    recoverTime(1)
{}
