#include "playerdata.h"

PlayerData::PlayerData()
  : maxspeed(110), minspeed(0.5), rotspeed(2.5), acceleration(75), hdamping(5), vdamping(2), mass(1),
    maxfitness(10), minfitness(0.6), endurance(260), type(0), r(30), recoverTime(20)
{}
