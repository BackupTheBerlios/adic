#include "playerdata.h"

PlayerData::PlayerData()
  : maxspeed(90), minspeed(0.5), rotspeed(1.5), acceleration(50), hdamping(2), vdamping(1), mass(1),
    maxfitness(10), minfitness(0.6), endurance(240), type(0), r(30), recoverTime(20)
{}
