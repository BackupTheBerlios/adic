#include "playerdata.h"

PlayerData::PlayerData()
  : maxspeed(60), minspeed(0.5), rotspeed(1), acceleration(40), hdamping(2), vdamping(1), mass(1),
    maxfitness(10), minfitness(0.3), endurance(80), type(0), r(30), recoverTime(30)
{}
